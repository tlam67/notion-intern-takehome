import commandHandler from "../src/utils/commandHandler.js";
import { jest } from "@jest/globals";

/**
 * @todo complete testing for commandHandler
 */

describe("commandHandler", () => {
  it("Implement later", () => {
    expect(true).toBe(true);
  });
});
