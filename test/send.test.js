import sendCommand from "../src/commands/send";
import { jest } from "@jest/globals";

/**
 * @todo complete testing for sendCommand
 */

describe("send", () => {
  it("Implement later", () => {
    expect(true).toBe(true);
  });
});
