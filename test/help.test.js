import helpCommand from "../src/commands/help";
import { jest } from "@jest/globals";

/**
 * @todo complete testing for helpCommand
 */

describe("help", () => {
  it("Implement later", () => {
    expect(true).toBe(true);
  });
});
