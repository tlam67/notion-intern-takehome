import deleteCommand from "../src/commands/delete";
import { jest } from "@jest/globals";

/**
 * @todo complete testing for deleteCommand
 */

describe("delete", () => {
  it("Implement later", () => {
    expect(true).toBe(true);
  });
});
