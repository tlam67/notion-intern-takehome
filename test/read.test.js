import readCommand from "../src/commands/read";
import { jest } from "@jest/globals";

/**
 * @todo complete testing for readCommand
 */

describe("read", () => {
  it("Implement later", () => {
    expect(true).toBe(true);
  });
});
